// export const hasSameKeys = (prev: KeyStroke[], next: KeyStroke[]) =>
//   prev.reduce((acc, cur, idx) => (equals(cur, next[idx]) ? acc : false), true);

// export const sameLength = (prev: Shortcut[], next: string[][]) =>
//   equals(length(prev), length(next));
